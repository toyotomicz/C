Zkouškové zadání obsahuje tři úkoly (TODO jsou v přiložených kódech):
    1) decoder.c        implementujte funkci `decode_message`                               20 bodů
    2) decoder.c        najděte chyby ve funkci `main`                                      5 bodů
    3) decoder.c        určete, kolik bytů informace je možné do obr. `jungle.bmp` ukrýt    5 bodů

Vytvořený program spuštěný příkazem:

build> .\steganodecoder.exe ../data/very_ordinary_jungle.bmp

by měl vypsat obsah tajné zprávy, která je v obrázku ukrytá.

3) (786486 - 54) / 8 = 98304 bits